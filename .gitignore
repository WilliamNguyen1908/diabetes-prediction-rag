# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# RAG build intermediates (regenerable from knowledge/pdfs via rag/convert.py).
# knowledge/index/ IS committed: the Dockerfile ships it, so a fresh clone can
# `docker compose up` without rebuilding the index.
knowledge/md/

# macOS junk
.DS_Store

# Local-only project notes
CLAUDE.md

# Secrets — API keys for the cloud eval judge
.env

# Audit log DB (may contain patient data)
recommendation_log.db
